# Toast icon crop validation

## `toast-q4-fy25`

- `toast-company-logo` — accepted after filtering the semi-transparent subscription-flow pixels that overlap the source logo's lower-left crop. The complete bread icon and `toast` wordmark remain centered with 20px horizontal and 15–17px vertical margins; the output contains no title, Revenue label, connector, or publisher mark. `crop-report.json` records `validation.passes: true`, zero edge/forbidden foreground pixels, and a non-zero transparent-background ratio.

The source-publisher `HOW THEY MAKE MONEY` mark, the `appeconomyinsights.com` URL, and the App Economy Insights footer attribution are intentionally skipped.
